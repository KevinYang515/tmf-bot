# Gap Burst Strategy — 試撮跳空慣性 Scalp

> 最後更新：2026-07-09（§3.5 門檻放寬測試(否決)+試撮外插修正(已部署)）／2026-07-04（§3.1 門檻/動態TP、§3.2 多空不對稱新因子、§3.3 兩個真bug、§3.4 秒級出場變體：候選中最優先）
> 狀態：**PAPER TRADE 中**（VM supervisor `trading-tmf-gap`，Shioaji simulation）
> 前身：`nq_strategy.py`（trading-tmf-nq，已退役，見文末）
> 給接手的 session/model：讀完本文件 + PLAYBOOK.md §0 就能接手。

---

## 1. 策略一句話

**開盤集合競價的跳空若是「最後一段時間的 surprise」（而非隔夜已消化的舊聞），開盤後有 1~3 分鐘的順向慣性 burst — 用試撮價在開盤前偵測，預掛市價單吃開盤價，TP+停損+時間上限快進快出。**

## 2. 定版參數（tick 級回測 2026-01~2026-07 定案）

| | 0845 日盤開盤 | 1500 夜盤開盤 |
|---|---|---|
| 參考收盤 | 夜盤收（今日 05:00 bar close） | 日盤收（今日 13:45 bar close） |
| surprise 窗口 | 05:00→08:45（3h45m，日韓開盤反應） | 13:45→15:00（75min，美股期貨） |
| 訊號 | 08:44:50 試撮價 vs 參考收 | 14:59:50 試撮價 vs 參考收 |
| 門檻 | \|gap\| ≥ **0.5%** | \|gap\| ≥ **0.3%** |
| 方向 | 多空都做 | **只做空**（跳空向上=多單不下，見 §3.2）（2026-07-04 起） |
| 進場 | 預掛 ROD/MKP 順 gap 方向 1 口 TMF | 同左 |
| TP | 固定 fill **+80pt** LMT | **動態**：fill + clip(0.4×\|fill-ref_close\|, 100, 300) LMT（2026-07-04 起） |
| 停損 | fill **-30pt**（tick 監控→MKP） | fill **-80pt** |
| 時間上限 | 開盤後 **300 秒**強制平倉 | 開盤後 **180 秒** |
| Backtest | n=19, EV **+281**/口, WR 57.9%, PF 2.87, worst -356 | 只做空 n=11, EV **+725**/口(動態TP), WR 100%(小樣本!), PF極高, worst -856（混合多空時 n=24 EV+281） |
| 年化頻率 | ~38 次 | 只做空後約 ~22 次（原 ~48 次的一半） |

成本假設：滑價 5pt（進場）+ 手續費稅 5.6pt/回合，均已含在 EV。
停損滑價敏感度：+10pt 惡化 → EV +239, PF 2.24（edge 存活）。
1500 動態 TP 細節與驗證見 §3.1；只做空的方向濾網細節與驗證見 §3.2（**注意 n=11 且 win 率 100%，見該節警語**）。

## 3. 為什麼是這些參數 — 研究結論總表（別重複造輪子）

以下全部測過並**否決**，接手者不要再走回頭路：

| 想法 | 結果 | 為什麼死 |
|---|---|---|
| gap vs **前日日盤收** 當訊號 | 275 組合僅 0.4% 正 EV | 夜盤已消化，開盤價 priced in |
| KOSPI/NKX 當獨立訊號 | PF < 1.0 | 同上 — 高相關 ≠ 有 follow-through edge |
| 試撮 gap 當 V0 (NQ) 的 filter | PF 1.44~1.54，輸 KOSPI filter | 開盤價已反映，無增量資訊 |
| TP-only 不停損（高勝率） | 勝率 83~94% 但 EV 負/脆弱 | 負偏態：一筆凹到收盤 -5K~-7K 吃掉 5~7 筆 TP |
| 順 gap + trailing 全日持有 | PF 0.6~0.9 | 慣性只活 1~3 分鐘，其餘是隨機漫步 |
| tick 級加 trailing（取代或疊加 TP） | 15 種變體全輸 fixed TP | 秒級路徑雜訊 20~40pt，trail 在雜訊裡被踢出 |
| NQ 當 1500 的 filter | corr(gap,NQ)=0.70，加了反而降穩健度 | gap 本身就是 NQ 的聚合，冗餘 |
| 點數門檻取代 % 門檻 | 正 EV 組合比例全面較低 | burst 尺寸跟波動(%)成比例 |
| 0845 動態 TP（gap 越大 TP 越大） | 全部輸固定 TP80（EV 96~298 vs 281） | gap 越大 MFE **反而略降**（見 §3.1 D1）|
| 0845 反向動態 TP（gap 越大 TP 越小） | 仍輸固定 TP80（EV 139~244 vs 281） | 固定 TP80 已能吃到大 gap 日的大部分行情，縮小 TP 反而封頂賺得少 |
| 停損隨 gap 動態 scaling（兩場都測） | 全部輸固定停損；worst 從 -356/-856 惡化到 -1200~-1550 | 拉寬停損=直接放大尾部風險，EV 換不回來 |

**唯一活下來的結構**：surprise gap（vs 夜盤收/日盤收）+ 順向 + TP(固定或依 gap 動態) + 固定停損 + 時間上限。
穩健度證據：0845 全組合 81% 正 EV、1500 帶停損組合 98% 正 EV，EV 矩陣平滑遞變（非孤峰）。

## 3.1 Overnight research pass (2026-07-04) — 完整記錄

**任務**：用近期/2026 資料回測「試撮 threshold 該多少」+「TP 是否該隨 gap % 動態調整」，過程中抓邏輯錯誤。

### 重大限制發現（先讀這個，會改變接手者的預期）
**Shioaji 歷史 `api.ticks()` 不保留開盤前試撮(simtrade)資料。** 用 2026-06-15、2026-07-03 兩天測試，
08:30-08:45 與 14:50-15:00 窗口回傳筆數皆為 0，且回傳欄位裡根本沒有 `simtrade` 欄位（只有
`ts/close/volume/bid_price/bid_volume/ask_price/ask_volume/tick_type`）。**結論：試撮讀值的準確度
永遠無法用歷史 replay 回測，只能靠 live 累積的 `gap_calibration.csv`**（目前僅 2 個樣本：0845 誤差
5pt、1500 誤差 53pt）。因此本次研究改用兩條路線代替：

**A) 門檻 sweep（用「實際 gap」，非試撮讀值）+ walk-forward (H1=01-03月 / H2=04-07月) 穩健性檢查**
- 0845：EV 隨門檻從 0.2%→0.6% 單調上升(+67→+291)，0.5~0.6% 是甜蜜點；但 H1(n=5) EV +524 遠高於
  H2(n=14) +194 — H1 樣本太薄，**真實 OOS EV 應該更接近 +194~+200，不是全樣本的 +281**。門檻本身穩健
  （0.35~0.6% 兩段都是正 EV），維持 0.5% 不動。
- 1500：0.3% 門檻 H1(n=12) EV +262 / H2(n=12) EV +239 — **兩半年幾乎相等，是全部門檻裡最穩健的一
  個**（其他門檻 H1/H2 落差都較大或樣本更薄）。維持 0.3% 不動。

**B) Walk-forward 交叉驗證（H1 選最佳組合，H2 純驗證，避免 275 組合 overfit 到假訊號）**
- 0845：H1 前 5 名組合全部落在 TP80，停損 15~80 都能在 H2 維持正 EV(+129~+194, PF 1.4~2.2) —
  TP80 是真的穩健，不是 overfit 到 5 個樣本。
- 1500：H1 前 5 名組合中，**現行 TP100/S80 在 H2 反而是表現最好的**(+239, PF 2.31)——不是挑出來的
  最佳值，是獨立驗證後仍最佳，信心較高。

**C) Noise-injection 穩健性測試（用合成雜訊代替無法取得的真實試撮誤差分布）**
把決策時看到的 gap 加上 N(0, noise_std) 雜訊，重算觸發率/方向翻轉率/EV：
- 0845：雜訊到 50pt 標準差前，方向翻轉率仍是 0%，EV 只從 +281 降到 +190；雜訊需到 ~80-120pt 才有感
  （方向翻轉 0.3~3%）。目前唯一真實樣本只有 5pt 誤差，**安全邊際很大**。
- 1500：更有趣——中等雜訊（10~30pt）EV 不降反升（+170→+192），因為讓一些臨界日跨過門檻反而是加分
  （呼應 A 段「門檻越低仍是正 EV」）；雜訊到我們唯一的真實樣本 53pt 時 EV 仍 +173（PF 健康），要到
  120~150pt 才開始真的吃緊（翻轉率 9~15%，EV 剩 +86~+118）。**1500 的試撮誤差安全邊際比 0845 薄**，
  值得持續觀察 `gap_calibration.csv`（任務 #46）。

**D) 動態 TP/停損延伸研究**
- D1（去除離群值後的相關性）：0845 的「gap 越大、MFE 越小」不是被單一極端日（04-08 gap 1173pt 但
  MFE 只有 33pt）拉歪的假象——去頭尾 15% 後相關係數從 -0.09 變成 **-0.52**，更負。1500 則穩定維持
  正相關(+0.35 全樣本 / +0.34 去尾)。兩場方向相反的結構是真的。
- D2（五分位輪廓）：0845 樣本量太小（每組 n=3~4），分位輪廓不單調、雜訊大，不適合擬合連續函數。
- D3（停損動態 scaling）：兩場都测过，全部輸固定停損，且 worst case 從 -356/-856 惡化到
  -1200~-1550 — **已否決，寫入 §3 表格**。
- D4（0845 反向動態 TP）：即使呼應了「大gap MFE 較小」的方向，實際套用「大gap用小TP」仍全面輸固定
  TP80（因為固定 TP80 在大 gap 日仍有 50~67% 機率吃到，縮小 TP 反而封頂）——**已否決**。
- D5（walk-forward 最終比較，2026-07-04 決策依據）：
  - 0845：固定 TP80 在 H2 (+194) 大幅勝過任何動態方案 (+115) → **維持固定 TP80**。
  - 1500：動態 TP = clip(0.4×\|gap_pts\|, 100, 300) 在 H1 打平固定值（因為 H1 沒有 gap 大到超過
    100pt 地板的日子）、在 **H2 明確勝出**（固定 TP100 得 +239，動態得 **+299**，PF 2.31→2.64）→
    **採用，已部署**（地板 100pt 保證任何情況都不劣於原本固定值）。

### 已實作變更（2026-07-04 部署）
1. `gap_strategy.py`：1500 session 新增 `tp_dynamic={"alpha":0.4,"lo":100,"hi":300}`，TP 用
   `clip(alpha×|fill_price-ref_close|, lo, hi)` 動態算，寫進 `gap_trades.csv` 的新欄位 `tp_used`。
   0845 維持固定 TP80（`tp_dynamic=None`，D5 驗證動態方案更差）。
2. **邏輯修正**：決策時若最後一筆試撮(`:44:50`/`:59:50`)剛好收不到值，原本會直接整場 SKIP，即使
   `:30/:40/:45` 早有明確讀值。已改為 fallback 用最近一筆有值的快照（純粹補救 tick 瞬斷，不影響
   正常情況下的行為——目前兩天 live 觀察 4 個快照都正常拿到，這是防禦性修正非急迫 bug）。
3. 停損 scaling **不採用**（D3 已否決，維持固定 -30 / -80）。
4. 進場滑價假設（backtest 用 -5pt）可能偏保守：0845/1500 進場都是「集合競價前掛 MKP」，理論上跟所
   有參與者拿同一個撮合價，真實滑價可能趨近於 0（而非 -5pt）。等下次真正觸發、比對 `gap_trades.csv`
   的 `fill_price` 是否等於 `gap_calibration.csv` 的 `actual_open` 即可驗證——若相等，代表回測其實偏
   保守，真實 EV 可能優於 backtest。

### 復現方式
```
D:\stock\tmf-bot\backtest\gap_overnight_research.py   Part A(門檻+walk-forward) / Part C(noise injection)
D:\stock\tmf-bot\backtest\gap_dynamic_tp.py            初版動態 TP 網格 (alpha × clip)
D:\stock\tmf-bot\backtest\gap_dynamic_tp2.py            D1~D5 完整延伸研究（相關性/分位/停損scaling/反向TP/walk-forward）
```

## 3.2 因子研究 pass 2（2026-07-04，同一晚接續 §3.1）

user 要求「繼續回測優化，找更多更好的因子與方向」。跑了 6 個新方向，**只有一個夠強、夠穩健到值得部署**——
其他都因為樣本太薄（n=2~13 的子分組）而只記錄不採用，避免重蹈「275 組合裡挑一個亮點」的覆轍。

### 唯一部署的發現：1500 只做空（跳空向上不下單）
用**已部署的動態 TP 基準**重新驗證（不是用舊的固定 TP100，避免結論對不上現在的設定）：

| | 跳空向上（做多） | 跳空向下（做空） |
|---|---|---|
| n | 13 | 11 |
| EV（動態TP, 全樣本） | **-95** | **+725** |
| EV（H1, 01-03月） | -66 | +497 |
| EV（H2, 04-07月） | -114 | +1,124 |
| 勝率 | 46% | 100%（見下方警語） |

做多方向在 H1、H2 兩段**都是負 EV**——不是被單一半年拖累的假象。就算掃遍 TP30~100 × cap60~180s 各種
組合試圖救回做多方向，找到的正 EV 組合也只有 EV+4~+31（見 `gap_asymmetry_verify.py` K3），跟做空的
+725 完全不是一個量級——**結論是「做多這個方向本身沒有 edge」，不是參數沒調好**。

**為什麼可能是真的（不只是巧合）**：1500 session 抓的是「日盤最後 75 分鐘的意外消息」。往下的意外
（賣壓、壞消息）常伴隨停損連鎖、恐慌性去槓桿，這類拋售有自我強化的動能，容易延續到晚盤；往上的意外
（買盤、好消息）較少有「被迫」的追價機制，開盤前的拉抬常常在真正開盤時就被消化完，後續 follow-through
較弱。這跟「跌時像瀑布、漲時像階梯」的市場常態相符，不是隨機巧合的說法有一定可信度。

**警語**：n=11、100% 勝率（0 筆虧損）。H2 只有 4 筆，這種「兩段都全勝」的乾淨程度統計上偏不尋常，
真實勝率長期應該會回落（也許 80~90%），但方向上「做多明顯弱於做空」這件事本身，在兩個獨立時間段都
成立，可信度足夠支撐部署。**接手者請把這個filter當作『高信心但小樣本』對待**：每多 5~10 筆做空交易，
就重新檢查一次 EV 有沒有跟回測的 +725 掉太多；如果做空也開始出現明顯虧損，要考慮是不是這個不對稱只是
2026上半年的特例，而不是結構性的。

同一批分析也重新驗證了 cap_seconds：舊版分析（用固定 TP100 當基準）誤以為縮短 cap 有幫助，但那其實
是被虧錢的做多交易混進去污染的假訊號——**改用『只做空+動態TP』的正確基準後，cap=180s（現行值）本身
就已經接近最優**（cap 90~210s 之間 EV 都在 +647~+725，180s 最高），**cap 不需要改動**。

### 測過但沒有部署的方向（樣本太薄，僅記錄）
| 因子 | 觀察 | 為何不部署 |
|---|---|---|
| 星期幾效應（0845/1500 都測） | 數字亂跳（1500週二 PF=inf n=4、週三 PF=0 n=2）；**0845 完全沒有週一樣本** | 每個 weekday bucket 只有 2~7 筆，純雜訊。**0845 沒有週一是 bug，不是巧合——見 §3.3** |
| 長假後效應（gap_days_since>=3） | 1500 長間隔後 EV 較弱(+61 vs 平時+329)，但這批日子幾乎等於「週一」，跟星期幾效應是同一件事被算兩次 | 本質是同一個弱訊號，且 n=7 太薄，暫不當獨立 filter |
| 舊聞gap與意外gap方向一致性(0845) | 方向不一致(反轉)時 EV 較高(+469 vs +144) | n=8 vs n=11，切太細 |
| 跨場：昨天1500方向預測今天0845 | 反向時 EV 較高(+377 vs +194) | n=6 vs n=10，切太細，且跟上一項可能是同一個「反轉>延續」現象的不同切法 |

這四個都指向同一個隱約的方向：**「反轉」訊號似乎比「延續」訊號可靠**（無論是舊聞vs意外反轉、還是跨
session反轉）。這是一個有潛力但目前證據力不足的假說，值得在樣本數翻倍後（幾個月後）重新檢驗，不建議
現在就動手實作 — 用 `gap_new_factors.py` 的 H/I 段重跑即可。

### 復現方式（新增）
```
D:\stock\tmf-bot\backtest\gap_new_factors.py        E~J 因子研究（星期幾/假日/多空/反轉/動態cap）
D:\stock\tmf-bot\backtest\gap_asymmetry_verify.py   K1~K4 多空不對稱用『現行動態TP基準』重新驗證
```

## 3.3 兩個真的 bug（2026-07-04，user 拿實際日期回頭問出來的）

user 拿 07/02、06/29、06/30 三天實際數字回頭質疑「結論是不是有誤會」，挖出兩個先前完全沒發現的問題——
不是統計雜訊，是程式邏輯本身的錯誤。**§3.2 表格裡「0845 樣本沒有任何週一」這件事，當時被誤記成正常
現象，其實是下面這個 bug 的直接後果。**

### Bug 1：週一 ref_close 抓不到（已修復）
`fetch_ref_close` 原本只查『今天』的 kbars。但週五 15:00 開的夜盤要跑到隔天 05:00 才收盤，這根
05:00 bar 的時間戳記其實落在**週六**，不是週一——查「週一」的 kbars 範圍完全找不到它，導致：
- **每一個週一**，0845 場的 `ref_close` 一律抓不到，log 顯示「資料缺→SKIP」，但其實資料存在，只是
  日期對不上查詢窗口
- 全年 118 個交易日中，**26 天（22%）完全無法算出 gap_night_pct，其中 24 天是週一**
- 這就是為什麼先前「星期幾效應」分析里 0845 完全沒有週一樣本——不是週一比較少跳空，是週一整場都被
  跳過，連判斷都沒發生過

**修復**：`fetch_ref_close` 改成查過去 4 天的窗口，取範圍內最後一根符合 (hour,minute) 的 bar，不管
它被標在哪一天。已部署。

**回填驗證**：用修好的邏輯回頭抓 26 天裡能抓到歷史 kbars 的 10 天（TMF 目前前月合約歷史資料只回溯
到約 4 月底，更早的日子 kbars 抓不到，這是資料可得性限制不是新 bug），其中 **6 天的 gap_night_pct
會過 0.5% 門檻**（05-04 +2.63%、05-11 -0.54%、05-18 -1.30%、05-25 +1.28%、06-01 +0.84%、06-15
+2.05%）——代表這半年 0845 本來有 6 筆完全被藏起來、從未進入任何統計的候選交易日。

### 但回測這 6 天，結果是負的——所以沒有跟著全面開放週一交易
抓了這 6 天的實際 tick，套用現行 TP80/S30/cap300：

| | 平日(原19天) | 週一(新發現6天) |
|---|---|---|
| n | 19 | 6 |
| EV | +281 | **-173** |
| 勝率 | 57.9% | **16.7%**（5戰4敗，只有1筆TP） |
| gap 幅度中位數 | ~0.7-0.8% | **1.29%**（更大！） |

週一的 gap 幅度反而更大（畢竟累積了整個週末），但表現卻更差——5 筆虧損裡有 2 筆(05-25, 06-15)後來
MFE 其實衝到 135-149pt（原本設定的TP80绰绰有余），但都是先觸停損才在事後才噴出，代表週一開盤的路徑
明顯更「震盪出場再噴出」，不是乾淨的單邊 burst。**這跟本策略最原始的結論完全吻合：「意外」訊號要新鮮
才有 burst 慣性，週一的 gap_night 本質上是「累積了 51+ 小時的舊聞」，資訊新鮮度已經跟「vs前日收的舊聞
gap」差不多，不是真正的 3h45m 級意外——這正是我們一開始就否決掉的那種訊號（見 §3 表格第一行）。

n=6 樣本很薄，這個「週一應該排除」的判斷信心不如 1500 多空分組（那邊 n=13/11 且前後半年都同號）。
但方向合理、跟核心理論一致，而且下修的代價很小（只是不下單，不影響其他場次），所以還是採用了。

**修復（已部署）**：`fetch_ref_close` 回傳 `(close, bar_date)`；若 bar_date 距今 >= 2 天（跨週末/連
假），標記 `is_stale_ref=True`，該場仍完整記錄 `gap_calibration.csv`（新增 `ref_stale_days` 欄位)，
但不下單。之後每週一 log 會顯示「參考價過舊 → 不下單（僅記錄校準資料）」。若之後樣本數增加、想重新
評估「週一是否真的該排除」，直接看 `ref_stale_days>=2` 的校準資料列即可。

### Bug 2：週末(六日)也會誤觸發（已修復）
主迴圈只檢查「現在時間是不是 08:42/14:57」，沒檢查今天是不是交易日。2026-07-04(六) 就實際誤觸發過
一次，log 顯示 ref_close 跟 4 個試撮快照全部是同一個值（市場沒開盤，snapshot fallback 抓到凍結的舊
報價），`actual_open` 永遠抓不到。因為湊巧 gap=0 沒有下單，沒造成實質影響，但這是不該發生的。
**修復**：主迴圈加一行 `if now_tp().weekday() >= 5: continue`，週六日直接跳過，不執行任何 session。

### 復現方式（新增）
```
D:\stock\tmf-bot\backtest\backfill_monday_ref.py      回填26個缺失日的正確gap_night_pct(需VM Shioaji)
D:\stock\tmf-bot\backtest\fetch_monday_ticks.py       抓6個新發現週一的tick資料
D:\stock\tmf-bot\backtest\gap_monday_backtest.py      6個週一回測 + 併入原19天重新驗證
（驗證週一05:00 bar被標在週六的探測腳本是一次性診斷，用完就刪了；
 重驗證邏輯：對任一contract呼叫 api.kbars(contract, start=跨週末日期, end=同一週末日期)，
 看05:00 bar落在哪一天即可重現）
```

## 3.4 秒級出場變體 — 從真實交易者成交明細學到的（2026-07-04）

user 提供了一位實際在做「開盤跳空 scalp」的交易者成交明細截圖（TXF 202607，06/29、06/30、07/02
三天 + 一筆虧損樣本 + 一筆 1500 夜盤場），這正是 user 發想本策略變體的靈感來源。從明細逆向工程出
他的做法，跟我們比對後有一個重要發現。

### 他的做法（從成交紀錄逆向）
- **進場跟我們完全一樣**：08:44:57~58 掛單（試撮最後 2~3 秒下決策）→ 吃集合競價開盤價。
  所有口數同一個成交價 = 開盤撮合價，證實「集合競價進場無滑價」（§3.1 第4點的猜想在他身上成立）
- **出場只抱 1~8 秒**：08:45:00 進、08:45:00~08 分批全出光（10~20 口分批限價 scale-out）
- **沒有 gap 門檻**：06/29(gap_night -0.45%)、07/02(-0.17%) 這種我們會 SKIP 的日子他都做
- **錯了秒砍**：虧損樣本顯示 -27 點 × 4 口、兩三秒內就認賠
- **1500 夜盤也做、多空都做**（他有一筆 15:00 開盤做多 1 秒內出光的紀錄）

### 為什麼他不用門檻我們要用 — 成本結構，不是訊號
TXF 點值 200、成本約 2.3pt/回合（成本占比 <8%）；TMF 點值 10、成本+滑價 10.6pt（占比 ~35%）。
用我們 68 天 tick 重算兩種成本結構下的「3 秒全出」：

| gap 區間 | TMF 成本結構 EV | TXF 成本結構 EV |
|---|---|---|
| <0.3% (n=30) | -46（做不起） | +540/口（約打平偏正，PF 1.16） |
| 0.3~0.5% (n=13) | -194 | -2,414（此區間本身就爛，見 §3 tiered TP 研究） |
| >=0.5% (n=25) | **+254** | +6,548/口（PF 4.79） |

**他「只要跳空就做」是理性的**：小 gap 日對他是接近免費的樂透（成本低到虧不了多少），大 gap 日
付全部的錢。同樣的行為換到 TMF 成本結構就是穩定虧錢 → **我們的 0.5% 門檻必須留著**。

### 重要發現：秒級出場在我們的門檻內比現行 TP80 更好（回測層面）
gap>=0.5%（現行門檻不變）、進場集合競價無滑價、出場市價滑價 5pt：

| 出場方式 | EV/口 | 勝率 | PF | worst | H1 / H2 |
|---|---|---|---|---|---|
| 現行 TP80/S30/cap300 | +172 | 48% | 1.93 | -356 | +524 / **+84** |
| **3秒全出 + 停損50** | **+264** | 64% | 3.67 | -606 | **+358 / +241** |
| 2秒全出 + 停損50 | +213 | 60% | 3.17 | -606 | +264 / +200 |
| 5秒全出 + 停損50 | +118 | 52% | 1.75 | -606 | +290 / +74 |

- 慣性最濃的其實是**頭 3 秒**，5 秒開始明顯稀釋——跟他實際 1~8 秒出光的行為吻合
- 3 秒版在 H1/H2 兩段都穩（現行版 H2 只剩 +84）
- 06/30 那筆從「開盤雜訊掃停損 -356」變 +1,194（3 秒內已出場，輪不到雜訊）

### 但尚未部署，三個保留
1. n=25、H1 只有 5 筆；3s vs 5s 差距大 = 對時間參數敏感，有 overfit 風險
2. 開盤頭 3 秒行情最猛，市價出場真實滑價未知（worst 那天 3 秒內反向噴 100 點），回測假設 5pt 可能偏樂觀
3. 他是大台分批限價出，我們單口市價出，執行細節無法完全複製

**行動方案**：現行版繼續 paper trade；等累積幾筆真實成交、用 `gap_trades.csv`/`gap_calibration.csv`
驗證「集合競價 fill 無滑價」與秒級出場滑價後，若與回測假設一致 → 切換到 3 秒版（改動僅三個參數：
cap 300s→3s、停損 30→50、TP 拿掉）。

### 補充驗證（同日稍晚）：1500 場不適用秒級出場；「無門檻」在 TMF 上不可行
1. **1500 場秒級出場全輸現行設定**：只做空+門檻0.3% 下，2s/3s/5s/10s 出場 EV +429~463，
   現行「動態TP/S80/cap180」是 +725（同 11 筆樣本）。1500 的 follow-through 是分鐘級
   （跌勢瀑布有延續），提早出場吃不完 → **1500 維持現行設定不動**。秒級出場只適合 0845
  （慣性真的只有頭幾秒）。兩場結構不同，不要套同一個出場。
2. **「只要跳空就做」(無門檻) × 3秒出場 × 三種合約成本**（0845，n=68 vs 門檻內 n=25）：

   | 成本結構 | 無門檻 EV/口 | 門檻0.5% EV/口 |
   |---|---|---|
   | TMF(點值10) | +21（H1 是負的） | +264 |
   | MXF(點值50) | +306 (PF 1.37) | +1,522 (PF 4.55) |
   | TXF(點值200) | +1,884 (PF 1.63) | +6,748 (PF 5.41) |

   TMF 無門檻不可行；MXF/TXF 無門檻「可活但單位期望值差 5~7 倍」——無門檻的意義只是
   加交易次數，每一筆的品質都被稀釋。且此測試樣本本身只含大波動日（tick 資料的篩選偏差），
   真正的無門檻會再多幾十個 gap≈0 的擲硬幣日，數字只會更差 → **門檻留著，無門檻不採用**。

### 補充驗證2（同日更晚）：週一用新出場法重驗、開盤逆向雜訊的真正成因
1. **6 個回填週一用「3秒出場+停損50」重測**：EV 從舊法的 -173 縮小到 **-38**，勝率
   16.7%→50%。新出場法讓虧損明顯縮小，**但仍是負的**——週一不下單的結論不變，只是
   量級被舊出場法高估了（05-25 這天用新法虧-326、用舊法虧-356，跟06/30同一種「開盤
   雜訊先洗一輪」的機制）。
2. **驗證「跳空越大，開盤越容易先出現秒級反向」的假設**：用 52 天資料量測「開盤後10秒
   內最大逆向偏移」vs |gap%|，**相關係數只有 0.175，很弱**。分組觸及-30停損比例在
   0.15~1.0% 各區間都在 43~61% 之間，沒有隨 gap 變大而單調上升——**真正的規律是
   「不管 gap 多大，開盤瞬間『先隨機洗一輪、再決定方向』本身就是常態（約一半天數會
   洗到-30），不是大 gap 特有的現象**。這解釋了「3秒出場」為什麼有效：不是靠加寬
   停損去扛過雜訊，是靠縮短曝險時間直接跳過雜訊發生的時間窗。gap>1%時逆向幅度確實
   放大（平均77pt），但那是極端日整體波動率放大的伴生效應，不是「越大越容易反向」
   的單調關係。

### 補充驗證3：精細出場時間 sweep + 兩個死路（一個真死路、一個曾誤判為活路）

**1. 精細 hold 秒數 sweep（1.0~5.0秒，每0.5秒一格，門檻0.5%內25天）**：1秒出場 EV **+324**
（勝率76%、PF 8.19），比3秒版(+264)還好，H1/H2 也相對接近(+402/+304，兩段差距比3秒版
更小)。**但曲線不是平滑遞減，是上下跳動的**(1.0s→1.5s反而從+324掉到+230，2.5s又回升到
+242)——n=25 加上0.5秒級距的時間軸，這種跳動本身就是過擬合警訊（tick成交時間戳記的隨機
性，在這麼細的切法下很容易只是巧合）。**1秒出場先當作有趣的旁證記錄，不當作比3秒版更可信
的結論**——3秒版的優勢是粗粒度sweep(2s/3s/5s)本身就單調，過擬合風險小得多。

**2. 死路：延遲武裝停損（開盤前X秒不判斷停損）完全沒有效果**——武裝延遲從0~2.5秒，EV
全部一樣是+264，一位小數點都不差。原因：停損設在-50時，3秒內幾乎不會真的觸及-50這麼深的
逆向，所以「什麼時候開始盯」根本不影響結果。真正有效的是「縮短總曝險時間」本身，不是
「延後開始防守的時間點」。**已排除，不用再測**。

**3. 曾誤判為活路，修正bug後打回原形：「開盤2秒確認、不利就提前出場」**。初版模擬把
「提前出場」寫成「當作沒進場、盈虧算0」——但集合競價一旦開盤撮合就已經成交，不可能事後
假裝沒進場，只能真的認賠出場。這是一個貨真價實的建模錯誤，不是措辭問題（user 直接點出
「這樣是否吃不到開盤市價單」才發現）。修正後（提前出場算真實虧損，不是0）：

| | EV/口 | worst |
|---|---|---|
| 基準：純3秒出場+停損50 | +264 | -606 |
| 修正前(錯誤)：confirm@2s>15pt提前出場 | +397 | -326 |
| **修正後(正確)：confirm@2s>15pt提前出場** | **+270** | **-576** |

修正後跟基準幾乎沒有差別（+264→+270，雜訊範圍內）——**「開盤2秒加一層確認再決定要不要
提前停損」沒有額外效益，已排除，不需要比純粹的固定出場秒數更複雜的邏輯**。

**教訓（給接手者）**：任何「跳過/放棄某天」的模擬邏輯，都要檢查是不是隱含了「可以事後
取消已成交的部位」這種不可能發生的假設。集合競價進場一旦撮合就是既定事實，後續只能选择
「續抱」或「認賠出場」，沒有第三個選項。

### 補充驗證4：實作細節 — 躺簿TP混合方案(否決) + 延遲敏感度 + 分批(否決) + 程式碼已備妥

**1. 躺簿TP + 3秒逾時混合方案 全部輸純3秒出場**：測了TP 30/40/50/60/80/100pt六種距離，
每一種都比「不掛TP、單純3秒到就全出」(EV+264)差(最好的TP100也只有+242)。原因：3秒本身
就是個很短的窗口，行情經常還沒走完，提早設停利點反而是賺得比它原本能給的還少。
**結論：不掛TP，時間到全部市價出場，是目前資料支持的最佳結構。**

**2. 出場滑價敏感度**：把假設的5pt滑價一路惡化到20pt(=0.2秒下單延遲若比預期糟4倍)，
EV從+264降到+114，**仍是正的**。0.2秒的下單延遲不是脆弱點，有相當大的容錯空間。

**3. 分批出場(部分停利)在數學上必然更差，不需要額外回測**：分批 = 把部位拆成兩半，
一半用某種提早出場方式、一半用純3秒出場，兩半損益取平均。因為前面已確定「任何提早出場
方式的EV都低於純3秒」，取平均只會把整體結果往下拉，不可能超過100%純3秒。真實交易者
用10~20口分批出，是因為她部位大、需要做風險分散，我們現在 POSITION_QTY=1，這個考量
不成立，即使之後加大部位，這個數學結論也不會變。

**4. 程式碼已備妥（2026-07-07），尚未部署**：`gap_strategy.py` 新增 `exit_mode` 設定
（"tp_cap" 現行邏輯 / "seconds" 新邏輯）+ `monitor_position_seconds()` 函式。**0845/1500
的 SESSIONS 設定都還是 `exit_mode="tp_cap"`，行為完全沒變**——切換只需要把某 session 的
`exit_mode` 改成 `"seconds"` 並設定 `hold_seconds`/`stop`，不需要動其他程式碼。新邏輯的
關鍵設計：進場基準直接用 tick 推播的開盤價（不等券商成交確認的2~12秒輪詢，那段輪詢在
3秒持倉尺度下會直接吃掉整個交易窗口）；不掛TP單；停損監控迴圈輪詢間隔改成
`min(剩餘秒數, 0.05)`，不能沿用原本3秒一次的粗輪詢（否則3秒的時間上限本身就會失準）。

### 復現方式
```
D:\stock\tmf-bot\backtest\gap_0845_hybrid_exit.py         躺簿TP+3秒混合方案(否決) + 出場滑價敏感度
D:\stock\tmf-bot\backtest\gap_0845_advanced_exit.py       精細hold秒數sweep + 延遲武裝停損(死路) + 提前出場(bug修正記錄)
D:\stock\tmf-bot\backtest\gap_monday_secexit_and_noise_corr.py   週一新出場法重驗 + 逆向雜訊相關性分析
D:\stock\tmf-bot\backtest\gap_no_threshold_seconds.py     無門檻×秒級×三種合約成本 + 1500秒級對照（本節補充驗證）
D:\stock\tmf-bot\backtest\gap_0845_simple_grid.py         乾淨的 TP/停損/cap/門檻全網格（含純時間出場對照）
D:\stock\tmf-bot\backtest\gap_0845_dyntp_low_threshold.py 「降門檻+動態縮TP」實驗（否決：小gap日怎麼縮都負）
D:\stock\tmf-bot\backtest\gap_0845_tiered_tp.py           「分段固定小TP」實驗（否決：35組合全掃無正EV）
D:\stock\tmf-bot\backtest\gap_0845_seconds_exit.py        秒級出場 × gap區間 × TMF/TXF成本結構對照
D:\stock\tmf-bot\backtest\gap_0845_seconds_wf.py          秒級出場 walk-forward + 停損混合版
D:\stock\tmf-bot\backtest\gap_0845_direction_asymmetry.py 0845多空不對稱測試（結論：不像1500，0845不需要只做單邊）
```

## 3.5 門檻放寬測試(否決) + 試撮外插修正(已部署) — 2026-07-09

user 看到 07/08 0845 SKIP（試撮 0.493% 差一點沒過 0.5% 門檻，但實際開盤 gap 是 0.595%），
問「要不要把門檻放寬成 0.49%/0.48%」。追下去發現這其實是兩個不同的問題，答案完全相反。

### 第一步：門檻放寬本身——細掃 0.01% 級距，結論是不要放寬

用 `gap_threshold_fine.py`（0845: 0.40~0.60%／1500: 0.20~0.40%，都是 0.01% 級距，
搭配 H1/H2 walk-forward）：

- **0845**：EV 隨門檻降低單調下滑（0.50%→0.40%：EV +281→+128），且「距門檻遠近」分桶
  顯示 **0.40~0.50%（低於現行門檻）這桶 n=6、勝率 0%、EV -356**——6 筆全部剛好停損出場，
  沒有一筆例外。放寬到 0.49% 只多抓 1 天（03-03），也是虧損；放寬到 0.48% 多抓 3 天，
  3 天全虧。**真實 gap 若真的 <0.5%，不是雜訊問題，是這個 burst 本身真的比較弱**（跟策略
  最初的核心理論一致：慣性強度跟意外幅度成正比）。
- **1500**：同樣單調，0.30%→0.20%：EV +725→+427；0.20~0.30% 桶 n=8、勝率 37.5%、EV 僅
  +16，遠遜於現行 0.30%+ 的 100% 勝率。**不放寬**。
- 附帶測了 0845 大 gap 端有沒有「太大反而更差」的天花板：1.50%+ 只有 n=1（04-08，
  gap+3.5%，虧損），樣本太薄無法下結論，暫不處理。

**結論：門檻本身（真實 gap）不能放寬，這部分驗證了現行 0.5%/0.3% 是對的，別再往這個方向想。**

### 第二步：user 追問「我們是靠試撮預判，本來就該有容錯」——這才是真正的問題

user 這句話點出的不是「真實 gap 門檻該降低」，而是「**試撮讀值本身是有偏誤的估計值，
用同一個門檻去卡一個有偏誤的估計值，會系統性漏掉真正過門檻的天**」——這是完全不同的
機制，用 `gap_calibration.csv` 累積的 8 筆真實樣本（0845/1500 各 4 筆）驗證：

| session | 日期 | 試撮(決策時) | 實際開盤 | 誤差方向 |
|---|---|---|---|---|
| 0845 | 07-03 | -0.009% | -0.020% | 低估 0.011pp |
| 0845 | 07-06(週一,stale) | +0.412% | +0.405% | 高估 0.006pp |
| 0845 | 07-07 | **-0.458%(SKIP)** | **-0.619%** | **低估 0.161pp** |
| 0845 | 07-08 | **+0.493%(SKIP)** | **+0.595%** | **低估 0.102pp** |
| 1500 | 07-03 | +0.223% | +0.111% | 高估 0.112pp |
| 1500 | 07-06 | +0.094% | +0.169% | 低估 0.075pp |
| 1500 | 07-07 | +0.297%(SKIP) | +0.343% | 低估(但跳空向上，方向濾網本來就不做) |
| 1500 | 07-08 | -0.143% | -0.143% | 0 |

0845 這 4 筆有 3 筆低估、且**兩筆本來會過門檻的真實觸發日（07-07/07-08）都因為試撮
讀值凍結在決策當下，被誤判成 SKIP**——這才是 user 說的「容錯」真正該解決的問題。
1500 誤差方向不一致（有低有高），沒有 0845 這麼清楚的偏態。

### 修法：不降門檻，改用試撮斜率外插到開盤時間點

看 07-07/07-08 這兩筆的 4 筆試撮快照，發現一個共同結構：最後兩筆快照(`:45→:50`)之間
價格還在朝 gap 方向移動，但決策用的是「凍結」在 `:50` 那一瞬間的讀值，沒有把「當下還在走、
10 秒後才真正開盤」這個時間差算進去：

- 07-07: `:45`=47160 → `:50`=47138（5秒內 -22pt），外插 10 秒到開盤 = 47138-44=47094，
  gap = -0.551%（過門檻）；實際開盤 47062，gap = -0.619%——方向對、量級接近。
- 07-08: `:45`=45426 → `:50`=45454（5秒內 +28pt），外插 10 秒 = 45454+56=45510，
  gap = +0.617%；實際開盤 45500，gap = +0.595%——同樣抓對。

**做法**：`gap_strategy.py` 新增 `project_trial()`，用 `snap_times` 最後兩筆的斜率
（`(trial_4-trial_3)/dt`）外插到 `open_time`，算出 `gap_projected_pct` 拿來做觸發判斷
（**門檻值本身不變**，0845 仍是 0.5%、1500 仍是 0.3%；TP/停損/下單方式完全不動——外插
只影響「要不要觸發」，不影響拿到 fill 之後的任何風控參數）。用全部 8 筆歷史校準資料
回測這個公式：**其餘 6 筆（斜率為 0 或早已遠離門檻）判斷完全不變，只有 07-07/07-08
這兩筆真正被誤判的日子會從 SKIP 變成正確觸發，没有引入任何新的誤觸發**。

```python
def project_trial(trials, snap_times, decide_time, open_time):
    if len(trials) < 2 or trials[-1] is None or trials[-2] is None:
        return trials[-1] if trials else None          # 資料不足 → 原樣退回，行為不變
    dt_last = _hms_to_sec(snap_times[-1]) - _hms_to_sec(snap_times[-2])
    dt_to_open = _hms_to_sec(open_time) - _hms_to_sec(decide_time)
    if dt_last <= 0:
        return trials[-1]
    slope = (trials[-1] - trials[-2]) / dt_last
    return trials[-1] + slope * dt_to_open
```

**已部署**（2026-07-08 深夜，VM `trading-tmf-gap` 已重啟生效）。`gap_calibration.csv`
新增 `gap_projected_pct` 欄位，跟原始 `gap_at_decide_pct`／`gap_actual_pct` 並列記錄，
方便後續持續比較外插值準不準。

**警語（比 §3.2 的 1500 多空 filter 更薄）**：這是 **n=2**（僅有的兩次真實誤判日）驗證出
來的修正，物理機制合理（試撮讀值本來就會在最後幾秒持續變動，凍結單一時間點本來就會有
延遲偏誤），下修代價低（絕大多數情況斜率為 0 或早已遠離門檻，行為不變；就算誤觸發，
停損機制不變，風險有上限）。但樣本極薄，**接手者請把接下來每一筆 `gap_projected_pct`
跟 `gap_actual_pct` 的差距記下來**：如果外插後的觸發判斷持續比原始試撮更接近實際開盤，
信心可以往上調；如果開始出現外插把本來該 SKIP 的日子誤判成觸發、且方向或幅度跟實際
開盤差很多，要考慮拿掉或改成更保守的外插係數（目前是滿額外插，可以改成打折，例如乘
0.5，需要更多樣本才能判斷）。

### 復現方式
```
D:\stock\tmf-bot\backtest\gap_threshold_fine.py     0.01%級距門檻細掃 + 距門檻分桶分析
D:\stock\tmf-bot\backtest\verify_projection.py      用 8 筆真實 gap_calibration.csv 樣本驗證外插公式
```

## 4. 為什麼散戶做得起（vs HFT）

- **進場 = 集合競價**：預掛市價單跟所有人拿同一個開盤撮合價，無速度競爭
- **TP = 躺簿 LMT**：晚 2 秒掛也無害（過價的限價單瞬間以更好價成交）
- **停損 = 唯一速度暴露**：tick→MKP 迴圈 ~0.2s，+10pt 滑價敏感度測試過活著
- Shioaji **沒有**觸價單/智慧單 API（永豐智慧單只在大戶投/eLeader），自建 tick 監控是唯一路

## 5. 部署細節

```
VM: 35.212.129.240  (gcloud compute ssh instance-20260515-172729 --zone=us-west1-b)
程式: ~/stock/gap_strategy.py  (repo: KevinYang515/tmf-bot)
Supervisor: trading-tmf-gap  (conf: /etc/supervisor/conf.d/trading-app.conf)
Cron: 每日 22:00 UTC restart（Shioaji token 日更），crontab 內 trading-tmf-gap
日誌: ~/stock/logs/gap_strategy.log (TimedRotating, 30天)
狀態: ~/stock/gap_state.json
帳號: Shioaji SIMULATION（.env 的 SJ_API_KEY/SJ_SECRET_KEY），與 V38 實盤完全隔離
```

**主迴圈時序**（時間全為 Taipei，程式內用 `now_tp()`，VM 是 UTC）：
```
08:42 / 14:57  prep 觸發 → kbars 抓參考收盤 → 訂閱 TMF tick
:44:30/:40/:45/:50 (或 :59:xx)  試撮快照 ×4 → 全部記入 gap_calibration.csv
:44:50 / :59:50  決策 → 過門檻就預掛 MKP
:45:00 / :00:00  開盤 → 捕捉第一筆真實 tick (=開盤價, 校準用)
+2s~12s  取 fill 價 → 掛 TP LMT → tick 停損監控啟動
+300s / +180s  時間上限 → 撤單 + MKP 平倉
```

**輸出 CSV**（在 VM `~/stock/logs/`）：
- `gap_calibration.csv` — **每天都寫**（不論觸發）：4 個試撮快照 + 實際開盤 + gap。用途見 §6
- `gap_trades.csv` — 每筆交易：方向/fill/出場原因/估 PnL

## 6. 接手者的任務清單

1. **每日看** `gap_strategy.log`：08:42~08:50、14:57~15:03 兩個窗口的行為是否正常
2. **首次觸發時驗證**：fill 價 vs 實際開盤價（滑價多少，順便驗證 §3.1 第4點的「集合競價滑價趨近0」猜想）、TP/停損/cap 行為是否照規格、1500 動態 TP 算出的 `tp_used` 是否合理（應在 100~300 之間）
3. ~~已知未驗證環節：sim 帳號 pre-open 是否推送 simtrade tick~~ — **已於 2026-07-03 首日驗證：可以**，4 個試撮快照皆正常捕捉。08:30-08:45/14:50-15:00 的試撮資料**無法事後回測取得**（Shioaji 歷史 API 不保留，見 §3.1），只能靠往後每天的 live 快照累積
4. **持續進行中**：分析 `gap_calibration.csv` — 試撮 :30/:40/:45/:50 vs 實際開盤的偏差分布。目前僅 2 筆（0845 誤差5pt / 1500 誤差53pt），§3.1 C 段的 noise-injection 分析顯示 1500 的誤差安全邊際比 0845 薄，累積到 10+ 筆後重新用真實分布取代合成雜訊分析，決定是否要加 buffer
5. **累積 10~20 筆後**：實測 EV vs 回測 EV（0845 +281 / 1500 只做空動態TP +725）。因為跳過了 2025 OOS（user 決定），**paper trade 就是 OOS** — 若實測明顯低於回測，優先懷疑 2026 過擬合（雖然 §3.1 B 段 walk-forward 交叉驗證顯示現行參數不是 overfit 到假訊號）
6. **TMF 簿薄風險**：回測用 MXF tick 模擬 TMF 成交，paper trade 記錄的真實滑價是關鍵校準
7. **1500 只做空 filter 是小樣本高信心發現（n=11, 100%勝率）**：每 5~10 筆做空交易後，重新檢查 EV 是否還接近 +725（見 §3.2）。若開始出現明顯虧損，考慮是不是 2026上半年的特例。1500 遇到跳空向上會在 log 顯示「方向濾網：只做空...→ 不下單」且 `gap_calibration.csv` 仍會記錄（`triggered=0`），只是 `gap_trades.csv` 不會有那筆交易——這是設計如此，不是漏單
8. **0845 週一目前不下單（`ref_stale_days>=2` 時 log 會顯示「參考價過舊→不下單」）**：這個判斷只基於 n=6 的回測（見 §3.3），信心比 1500 的多空 filter 更弱。累積幾個月的 `gap_calibration.csv`（篩 `ref_stale_days>=2` 的列）後，重新評估週一是否真的該排除，或者只是需要不同的 TP/停損（目前沒測過週一專屬參數，因為 n=6 連測都不夠）
9. **秒級出場變體（§3.4）是目前最優先的參數候選**：回測 EV +264 vs 現行 +172 且 H1/H2 都穩。等首批真實成交驗證「集合競價 fill 無滑價」+ 秒級市價出場滑價後決定是否切換（cap 300s→3s、停損 30→50、TP 拿掉）。驗證方法：比對 `gap_trades.csv` 的 fill_price vs `gap_calibration.csv` 的 actual_open，以及出場價 vs 出場當下 tick
10. **試撮外插（§3.5，2026-07-09 部署）是 n=2 的修正，優先追蹤**：每天檢查 `gap_calibration.csv` 新增的 `gap_projected_pct` 欄位跟 `gap_actual_pct` 的差距是否比原始 `gap_at_decide_pct` 更接近；特別留意有沒有「外插把不該觸發的日子誤判成觸發」的案例（斜率雜訊被錯誤放大）。若外插持續有效可以考慮把 §3.5 的門檻放寬否決結論也一併重新檢查（因為外插等於變相讓臨界日更容易被正確分類，跟直接降門檻不是同一件事，但要留意兩者交互作用）

## 7. Rollback

```bash
# 回復 NQ 策略（gap 策略下架）
sudo sed -i -e 's/program:trading-tmf-gap/program:trading-tmf-nq/' \
  -e 's|stock/gap_strategy.py|stock/nq_strategy.py|' \
  -e 's|tmf_gap|tmf_nq|g' /etc/supervisor/conf.d/trading-app.conf
crontab -l | sed 's/trading-tmf-gap/trading-tmf-nq/' | crontab -
sudo supervisorctl reread && sudo supervisorctl update
# 舊 conf 備份: /etc/supervisor/conf.d/trading-app.conf.bak_gapswap
```

## 8. 前身 nq_strategy 的結局（歷史）

- v2(B)：NQ 動能雙 session。1500 最後一週 3 進 3 停損 (-4,600)
- v4：0845 加 KOSPI filter（backtest Sharpe 7.43）— 部署 6 天零觸發，未經實測即被本策略取代
- 檔案保留：`nq_strategy.py`（repo + VM），`NQ_TMF_STRATEGY.md`
- NQ/KOSPI 研究的教訓已吸收進本策略：外圍指數的資訊會被開盤價 price in，**當 filter 有用、當訊號沒用**；而試撮 gap 連 filter 都不用 — 它本身就是聚合了所有外圍資訊的 surprise 量測

## 9. 回測重現

```
本機 D:\stock\tmf-bot\backtest\（python: stock312 env）
資料: gap_ticks\ (0845, 61天) / gap_ticks_1500\ (71天) — Shioaji api.ticks 抓的 MXF 開盤窗口
  重抓: scratchpad 的 fetch_gap_ticks.py / fetch_1500_ticks.py 模式（在 VM 跑, kbars 一次限 30 天）
主要腳本:
  gap_tick_sweep.py         0845 全網格 (275 組合 × 4 篩選)
  gap_tick_sweep_1500.py    1500 全網格 + NQ filter + %vs點數
  gap_with_stop_only.py     強制帶停損排行（定版依據）
  gap_add_trailing.py       trailing 疊加測試（全輸）
  gap_scalp_horizon.py      慣性壽命 (drift @ k分鐘 + MFE/MAE)
  gap_tponly*.py            TP-only 陷阱的完整拆帳
  stop_slip_sensitivity.py  停損滑價敏感度
  gap_overnight_research.py 2026-07-04: 門檻walk-forward + noise-injection 穩健性 (Part A/B/C)
  gap_dynamic_tp.py         2026-07-04: 動態 TP 初版網格
  gap_dynamic_tp2.py        2026-07-04: 動態TP/停損 D1~D5 完整延伸研究（見 §3.1）
```
